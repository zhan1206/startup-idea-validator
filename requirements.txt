# Core dependencies
# Standard library only, no external packages required!

# Optional: for better formatting
colorama>=0.4.6
rich>=13.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Type checking
mypy>=1.0.0
